import React from 'react';

import StoriesList from '@/components/StoriesList';

export const Home = () => {
  return (
    <React.Fragment>
      <StoriesList />
    </React.Fragment>
  );
};

export default Home;
